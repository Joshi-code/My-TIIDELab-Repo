git command for staging a change = git add [file-name] or git add .
git command for committing and change = git commit -m "commit message"
git command for pushing a committed change = git push -u origin main/master